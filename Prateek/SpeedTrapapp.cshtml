
@{
    Layout = null;
}

<!DOCTYPE html>

      <!--Script references. -->
   
    <script src="Scripts/jquery-1.6.4.min.js" ></script>
    <!--Reference the SignalR library. -->
    <script src="Scripts/jquery.signalR-2.1.0.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="signalr/hubs"></script>
    <!--Add script to update the page and send messages.--> 
   
<script type="text/javascript">
    $(function () {
        // Declare a proxy to reference the hub. 
        var chat = $.connection.Hubs;
        // Create a function that the hub can call to broadcast messages.
        chat.client.broadcastMessage = function (name, speed) {
            var encodedName = $('<div />').text(name).html();
            var encodedSpeed = $('<div />').text(speed).html();
            alert(encodedSpeed);;
        }
    });
</script>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>SpeedTrack</title>
</head>
<body>
   <div class="container">
       <a style="font-size:larger; color:green">Speed Trap</a> Overspeed <br />
        <input type="text" id="message" />
        <input type="button" id="sendmessage" value="Apply" />
        
    </div>
    <div>

    </div>
</body>
</html>
